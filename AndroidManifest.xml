<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geely.geely"
    android:versionCode="7"
    android:versionName="0.3" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" android:hasCode="true" android:persistent="false">
        <activity
            android:name="com.geely.geely.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.geely.geely.Catalog"></activity>
        <activity android:name="com.geely.geely.MenuWin">
        </activity>
        <activity android:name="Search"></activity>
        <activity android:name="SearchFull"></activity>
    </application>

</manifest>